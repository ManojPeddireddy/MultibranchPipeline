
pipeline {
    agent any

    stages {
        
        stage('Build') {
            steps {
                sh 'git branch'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo "Hello World"'
            }
        }
    }
}
